# MacPorts Installer addition on 2011-11-06_at_14:20:16: adding an appropriate PATH variable for use with MacPorts.
export PATH=/opt/local/bin:/opt/local/sbin:$PATH
# Finished adapting your PATH environment variable for use with MacPorts.
alias rasp="nmap -sV -p 22 192.168.0.10/20 | grep -B1 -A3 'Host is up' | grep -B4 'Service Info: OS: Linux' | head -n1 | cut -d ' ' -f5"


init_module=/usr/local/Modules/3.2.10/init/bash
if [ -d "$init_module" ] ; then
    . $init_module
fi
